CXX_STD = CXX11

PKG_CXXFLAGS += $(SHLIB_OPENMP_CXXFLAGS) -DRCPP_PARALLEL_USE_TBB -DARMA_64BIT_WORD -DARMA_USE_OPENMP

PKG_LIBS += $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" \
              -e "RcppParallel::RcppParallelLibs()") $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(SHLIB_OPENMP_CXXFLAGS)
